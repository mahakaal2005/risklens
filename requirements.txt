 fastapi==0.141.1
 uvicorn[standard]==0.52.4
 pydantic==2.13.5
 sqlalchemy==2.0.52
 # Postgres driver -- only exercised when DATABASE_URL points at Postgres
 # (e.g. on Render). SQLite (the local-dev default) needs no extra driver.
 psycopg2-binary==2.9.12
 streamlit==1.63.0
 pandas==2.3.3
 numpy==2.5.2
 scikit-learn==1.9.0
 # Used only by the comparison-only trajectory Transformer baseline
 # (ml/trajectory_transformer.py). Every live-scoring path is scikit-learn.
 torch==2.14.0
 pyyaml==6.0.3
 joblib==1.6.0
 pytest==8.4.2
 httpx==0.28.1

# NOTE: google-cloud-aiplatform (Vertex AI, for the dashboard's optional
# Gemini evidence-analysis call) is intentionally NOT listed here. It pulls
# in a large, loosely-versioned transitive dependency tree that makes pip's
# resolver extremely slow -- and it's only used by
# dashboard/components/case_detail.py, never by the API service. Installing
# it unconditionally in this shared file previously broke the API service's
# build entirely (pip: "resolution-too-deep" after 15+ minutes of
# backtracking). See requirements-dashboard.txt, which layers it on top of
# this file for the dashboard service only -- the API service installs just
# this file and never needs it.
