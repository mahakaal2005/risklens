# Local environment
.env
__pycache__/
*.pyc
.pytest_cache/

# Local SQLite database
*.db

# External benchmark dataset -- never commit. See docs/EXTERNAL_BENCHMARK.md.
data/external/

# Uploaded evidence attachments (Phase 2, local demo only) -- never commit.
data/evidence_attachments/

# Local AI-assistant tooling config/instructions -- kept locally, never committed.
CLAUDE.md
.mcp.json
.claude-flow/
.claude-skills-log.jsonl
scripts/log-skill-usage.sh
